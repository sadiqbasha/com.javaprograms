package com.javaprograms;


import java.util.List;
import java.util.Arrays;
public class NavigateDynamicUrls {

	public static void main(String[] args) {
		/* Set up WebDriver (Make sure to set the path to your chromedriver)
        System.setProperty("webdriver.chrome.driver", "path/to/chromedriver");
         ChromeDriver driver = new ChromeDriver();

        // Base URL
        String baseUrl = "https://example.com/";

        // List of dynamic parts (endpoints)
        List<String> endpoints = Arrays.asList(
            "page1",
            "page2",
            "products/item1",
            "products/item2",
            "category/sports",
            "category/tech",
            "blog/article1",
            "blog/article2",
            "contact",
            "about-us"
        );

        // Loop through each endpoint and navigate
        for (String endpoint : endpoints) {
            String fullUrl = baseUrl + endpoint;  // Construct full URL
            driver.get(fullUrl);  // Navigate to constructed URL
            System.out.println("Navigated to: " + fullUrl);           
        }

        // Close the browser
        driver.quit(); */


	}

}
